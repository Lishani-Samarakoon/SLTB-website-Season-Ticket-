services:
  backend:
    build: ./server
    ports:
      - "5000:5000"
    restart: unless-stopped
    environment:
      - PORT=5000

  frontend:
    build: .
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: unless-stopped